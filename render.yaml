services:
  - type: web_service
    name: owner-avatar-logistics
    env: php
    branch: main
    root: /
    buildCommand: |
      # install admin UI deps only
      cd admin && npm ci --prefer-offline --no-audit
    startCommand: "php -S 0.0.0.0:$PORT index.php"
    healthCheckPath: /
